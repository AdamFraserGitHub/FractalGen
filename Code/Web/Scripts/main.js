var canvas = document.getElementById("canvasMain"),
    frame = canvas.getContext("2d");

// stepSquareDraw(1000, 20, 50);
// internalisedCircleDraw(1000,8);
serpinskiDraw(1000, 1000, 3); //needs work as in it doesnt work at all